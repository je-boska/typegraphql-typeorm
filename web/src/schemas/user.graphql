mutation Login($data: LoginUserInput!) {
  login(data: $data) {
    user {
      name
      email
    }
    errors {
      field
      message
    }
    token
  }
}

mutation Register($data: RegisterUserInput!) {
  register(data: $data) {
    user {
      name
      email
    }
    errors {
      field
      message
    }
    token
  }
}
