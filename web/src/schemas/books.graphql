query Books {
  books {
    id
    title
    author
    isPublished
  }
}

mutation CreateBook($data: CreateBookInput!) {
  createBook(data: $data) {
    id
    title
    author
    isPublished
  }
}
